--- a/tools/tar/Makefile
+++ b/tools/tar/Makefile
@@ -16,6 +16,8 @@ HOST_FIXUP := autoreconf
 
 HOST_BUILD_PARALLEL := 1
 
+HOST_CONFIGURE_VARS += gl_cv_func_getcwd_path_max=yes
+
 include $(INCLUDE_DIR)/host-build.mk
 
 $(eval $(call HostBuild))
